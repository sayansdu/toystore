<!DOCTYPE mapper     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="UserMapper">

    <select id="selectUser" resultType="entity.User" parameterType="string">
        select * from k_user where email = #{email}
    </select>
    <insert id="insertUser" parameterType="entity.User">
      insert into k_user (name, surname, password, email, status) values(
      #{name, jdbcType=VARCHAR, mode=IN},
      #{surname, jdbcType=VARCHAR, mode=IN},
      #{password, jdbcType=VARCHAR, mode=IN},
      #{email, jdbcType=VARCHAR, mode=IN},
      #{status, jdbcType=VARCHAR, mode=IN}
      )
    </insert>
<!--    <insert id="insertUser"  parameterType="entity.User">
        select INSERTUSER(
          #{name, jdbcType=VARCHAR, mode=IN},
          #{surname, jdbcType=VARCHAR, mode=IN},
          #{password, jdbcType=VARCHAR, mode=IN},
          #{email, jdbcType=VARCHAR, mode=IN}
        ) from dual
    </insert>-->

    <select id="selectGoods" resultType="entity.Goods">
        select * from k_goods
    </select>
    <select id="selectGoodsById" resultType="entity.Goods" parameterType="long">
        select * from k_goods where id = #{id}
    </select>
    <insert id="insertGood" parameterType="entity.Goods">
      insert into k_goods (name, description, category, price, amount, foto_dir) values(
      #{name, jdbcType=VARCHAR, mode=IN},
      #{description, jdbcType=VARCHAR, mode=IN},
      #{category, jdbcType=VARCHAR, mode=IN},
      #{price, jdbcType=INTEGER, mode=IN},
      #{amount, jdbcType=INTEGER, mode=IN},
      #{photoDirectory, jdbcType=VARCHAR, mode=IN}
      )
    </insert>
    <delete id="deleteGood" parameterType="long">
        delete from k_goods where id = #{id}
    </delete>
    <update id="updateGood" parameterType="entity.Goods">
        update k_goods set
          name = #{name},
          description = #{description},
          category = #{category},
          price = #{price},
          amount = #{amount}
          where id = #{id}
    </update>

    <select id="getBuyerAmount" resultType="integer">
        select TOTALUSERS from dual
    </select>

    <select id="singIn" resultType="integer" parameterType="entity.User">
        select SIGNIN(#{email}, #{password}) from dual
    </select>

</mapper>